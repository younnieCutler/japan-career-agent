{
  "name": "career-workspace-frontend",
  "private": true,
  "type": "module",
  "description": "Career Workspace GUI. Built output is committed to skills/career-agent/gui/static/app so the Python wheel ships it and users never need Node.",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "@seed-design/css": "^2.5.0",
    "@seed-design/react": "^2.3.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.0",
    "vite": "^8.2.1"
  }
}
